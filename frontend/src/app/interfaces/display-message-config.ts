export interface DisplayMessageConfig {
    showAlways: boolean,
    title: string,
    image: string,
    icon: string,
    message: string,
    button: string,
    secondaryButton?: string,
    delay: number,
    showSpinner: boolean,
    autoclose: boolean
}
