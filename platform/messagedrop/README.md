# MessageDrop public site

Static marketing/legal pages for `https://messagedrop.de` are generated into `platform/messagedrop/public`.

Build from this directory with:

```bash
npm run build
```

The web app itself is deployed separately under `https://app.messagedrop.de`.
